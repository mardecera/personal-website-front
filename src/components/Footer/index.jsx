import styles from  './index.module.css'

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <span>{'&copy'}</span>
            <span>mardecera 2022</span>
        </footer>
    );
}
 
export default Footer;